import unittest
from unittest.mock import patch

class TestSumOfElements(unittest.TestCase):
    @patch('__main__.sum_of_elements')
    def test_sum_of_elements(self, mock_sum_of_elements):
        mock_sum_of_elements.return_value = 15
        array = [1, 2, 3, 4, 5]
        self.assertEqual(sum_of_elements(array), 15)
        mock_sum_of_elements.assert_called_once_with(array)

if __name__ == "__main__":
    unittest.main()